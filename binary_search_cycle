#Binary search realisation using cycle 

#Definition of binary search function
def binary_search(item, list):
    low = 0 
    high = len(list_values) - 1
    while low <= high:
        mid = (low + high) / 2
        mid = int(mid)
        guess = list[mid]
        if guess > item:
            high = mid - 1
        elif guess == item:
            return mid
        else:
            low = mid + 1
